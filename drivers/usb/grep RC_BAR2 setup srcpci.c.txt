grep "RC_BAR2 setup" src/pci.c
# Must print: RC_BAR2 setup (1GB inbound DMA, PCIe 0xC0000000 -> CPU 0x0)
# NOT:        RC_BAR2 setup (1GB inbound DMA, PCIe base 0xC0000000)

grep "writel.*MISC_RC_BAR2_CFG_LO" src/pci.c  
# Must print: writel(0xC0000012U, ...
# NOT:        writel(0xFFFFFFFFU, ...

grep "EINT set after HCRST" src/usb_xhci.c
# Must find a match