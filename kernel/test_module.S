/* test_module.S — PhoenixTest AArch64 native module binary
 * Generated by CMunge 0.84 -64bit -tgcc, compiled for Cortex-A72.
 * boot392: first AArch64 native module built via CMunge pipeline.
 *
 * Header (from binary):
 *   +0x04 init_entry = 0x40000078  (bit30=1 AArch64, offset=0x78)
 *   +0x10 title_ptr  = 0x34        -> "PhoenixTest"
 *   +0x30 flags ptr  = 0x70        -> 0x14 (AArch64 ISA + zero-init present)
 */

    .section .test_module, "a", @progbits
    .balign 4

    .global test_module_start
test_module_start:
    .incbin "kernel/PhoenixTest.bin"
    .global test_module_end
test_module_end:
